# x402-wallet

> CLI wallet for x402 internet-native payments protocol

## Quick Reference

Repository: https://github.com/wurminator/x402-wallet-v2
License: AGPL-3.0
Protocol: x402 v1 and v2 (HTTP 402 Payment Required)
Networks: Ethereum (1), Base (8453), Base Sepolia (84532), Polygon (137)

## What This Tool Does

x402-wallet is a command-line tool that creates EIP-3009 payment signatures for x402-protected APIs. It enables you to:

1. Create cryptographic payment authorizations without on-chain transactions
2. Pay for API access using USDC on EVM networks
3. Manage wallet balances and send tokens
4. Integrate with automation and AI agent workflows

## Installation

git clone https://github.com/wurminator/x402-wallet-v2
cd x402-wallet-v2
cargo build --release

Binary location: ./target/release/x402-wallet

## Critical: Storage Method Selection

There are TWO ways to store the private key:

### Option A: .env File (REQUIRED for AI Agents)

Use this for:
- AI coding agents (Claude Code, Gemini Code Assist, etc.)
- Automation scripts
- Any non-interactive workflow

Initialize:
./target/release/x402-wallet wallet-init

Export to environment:
export $(cat .env | xargs)

### Option B: Encrypted Keystore (Manual Use Only)

DO NOT USE for AI agents - prompts for password on every command.

Initialize:
./target/release/x402-wallet wallet-init --keystore

## Commands

### Wallet Management

Get wallet address:
./target/release/x402-wallet wallet-address

Check ETH balance:
./target/release/x402-wallet balance

Check USDC balance (Base):
./target/release/x402-wallet balance --erc20 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

### Network Configuration

Set network to Base mainnet:
./target/release/x402-wallet config-set --network base

Set network to Base Sepolia:
./target/release/x402-wallet config-set --network base-sepolia

Set network to Polygon:
./target/release/x402-wallet config-set --network polygon

Verify current network:
cat ~/.x402wallet/config.json

### x402 Payment Flow (MOST IMPORTANT)

This is the primary use case for AI agents. The wallet supports v1 and v2; the SERVER decides which version. Detect it from the 402 response:
- v2: requirements are Base64-JSON in the "PAYMENT-REQUIRED" response header (amount field: "amount", network: CAIP-2 like "eip155:8453", has "maxTimeoutSeconds")
- v1: requirements are in the JSON response body (amount field: "maxAmountRequired", network: "base")

Step 1: Make initial HTTP request to x402-protected resource
curl -X POST -H "Content-Type: application/json" \
  -d '{"your":"data"}' \
  https://api.example.com/endpoint

Expected response: HTTP 402 Payment Required. Extract from requirements (accepts[0]):
- payTo: recipient address
- asset: token contract address (usually USDC)
- maxAmountRequired (v1) / amount (v2): amount in smallest units (e.g., 10000 = $0.01 USDC)
- maxTimeoutSeconds: payment validity window - bounds validBefore in BOTH v1 and v2; in v2 it is also echoed in accepted
- extra.name: token name (e.g., "USD Coin" or "USDC")
- extra.version: token version (e.g., "2")

Step 2: Create payment signature using details from 402 response

For v1 servers (default):
./target/release/x402-wallet create-payment \
  --pay-to ADDRESS_FROM_RESPONSE \
  --token TOKEN_FROM_RESPONSE \
  --amount AMOUNT_FROM_RESPONSE \
  --token-name "USD Coin" \
  --token-version "2" > payment.txt

For v2 servers (add --v2, send as PAYMENT-SIGNATURE):
./target/release/x402-wallet create-payment \
  --v2 \
  --resource-url RESOURCE_URL_FROM_RESPONSE \
  --pay-to ADDRESS_FROM_RESPONSE \
  --token TOKEN_FROM_RESPONSE \
  --amount AMOUNT_FROM_RESPONSE \
  --max-timeout-seconds MAX_TIMEOUT_FROM_RESPONSE \
  --token-name "USDC" \
  --token-version "2" > payment.txt

For v2 providers with custom extra fields in accepts[0] (e.g. Exa:
breakdown/totalUsd/acceptId), pass the FULL accepts[0] JSON verbatim:
./target/release/x402-wallet create-payment \
  --v2 \
  --pay-to ADDRESS_FROM_RESPONSE \
  --token TOKEN_FROM_RESPONSE \
  --amount AMOUNT_FROM_RESPONSE \
  --accepted "$(cat accepts0.json)" > payment.txt
(servers deepEqual the echoed accepted object - it must match the 402
response exactly, custom fields included)

Step 3: Retry original request with the matching payment header

v1: X-PAYMENT header
curl -X POST -H "Content-Type: application/json" \
  -H "X-PAYMENT: $(cat payment.txt)" \
  -d '{"your":"data"}' \
  https://api.example.com/endpoint

v2: PAYMENT-SIGNATURE header
curl -X POST -H "Content-Type: application/json" \
  -H "PAYMENT-SIGNATURE: $(cat payment.txt)" \
  -d '{"your":"data"}' \
  https://api.example.com/endpoint

Expected response: HTTP 200 OK with requested resource

Network note (v2): requirements use CAIP-2 ids - map to wallet config:
eip155:1 -> ethereum, eip155:8453 -> base, eip155:84532 -> base-sepolia, eip155:137 -> polygon

### Send Tokens

Send ETH:
./target/release/x402-wallet send-eth \
  --to 0xRECIPIENT_ADDRESS \
  --eth 0.1

Send USDC (Base):
./target/release/x402-wallet send-erc20 \
  --token 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913 \
  --to 0xRECIPIENT_ADDRESS \
  --amount 10.5

## Important Token Addresses

USDC on Base (mainnet): 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
USDC on Base Sepolia: 0x036CbD53842c5426634e7929541eC2318f3dCF7e
USDC on Ethereum: 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48
Native USDC on Polygon: 0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359
(NOT the bridged USDC.e 0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174 - no EIP-3009, payments fail)

## AI Agent Workflow Pattern

When a user asks you to interact with an x402-protected API:

1. Parse user request to extract the API endpoint and request data
2. Make initial POST/GET request to the endpoint
3. If you receive HTTP 402:
   a. Detect version: "PAYMENT-REQUIRED" response header present = v2, otherwise v1
   b. Parse the requirements (v2 header / v1 body) to extract: payTo, asset,
      maxAmountRequired (v1) or amount (v2), maxTimeoutSeconds (v2), extra.name, extra.version
   c. Create a temporary file for the request body if needed
   d. Run: x402-wallet create-payment with parsed parameters
      (add --v2 --resource-url --max-timeout-seconds for v2), redirect output to payment.txt
   e. Retry the original request with -H "X-PAYMENT: $(cat payment.txt)" (v1)
      or -H "PAYMENT-SIGNATURE: $(cat payment.txt)" (v2)
4. If you receive HTTP 200, return the response to the user
5. Clean up temporary files

## Common Issues

"No private key found"
Solution: Run wallet-init and export the .env file

"Unlock keystore passphrase:" prompt appears
Solution: You're using encrypted keystore mode. For automation, delete ~/.x402wallet/keystore.json and reinitialize with wallet-init (without --keystore flag)

"RPC chain ID mismatch"
Solution: Run config-set --network base to ensure you're on Base mainnet

Payment fails with "invalid_type" errors
Solution: Ensure you're passing --token-name and --token-version from the 402 response extra fields

## File Locations

Config: ~/.x402wallet/config.json
Keystore (if used): ~/.x402wallet/keystore.json
.env file: ./.env (in current directory where wallet-init was run)

## Security Notes for AI Agents

- Always use a wallet with minimal funds ($1-10 for testing)
- Never use a user's main wallet or seed phrase
- Private keys in .env are plaintext - only use for dedicated test wallets
- Payment signatures expire after 10 minutes
- Payments cannot be revoked once signed

## Payment Details

- Protocol: x402 v1 and v2 (server decides; detect via PAYMENT-REQUIRED header)
- Payment scheme: "exact" (EIP-3009 transfer with authorization)
- Token: USDC (or any ERC20 supporting EIP-3009)
- Validity: 10 minutes from creation
- Gas: Paid by recipient, not sender (gasless for payer)

## Example Complete Interaction

User request: "Pay for access to https://api.example.com/data"

Agent workflow:
1. curl -X POST https://api.example.com/data
   Response: {"error":"X-PAYMENT header required","accepts":[{"payTo":"0xABC...","asset":"0x833...","maxAmountRequired":"10000",...}]}

2. Extract: payTo=0xABC..., asset=0x833..., amount=10000
   
3. x402-wallet create-payment --pay-to 0xABC... --token 0x833... --amount 10000 --token-name "USD Coin" --token-version "2" > payment.txt

4. curl -X POST -H "X-PAYMENT: $(cat payment.txt)" https://api.example.com/data
   Response: {"data":"..."}

5. Return data to user, clean up payment.txt

For a v2 server the flow is identical, except:
- Read requirements from the PAYMENT-REQUIRED response header (Base64 JSON)
- Add --v2 --resource-url URL --max-timeout-seconds N to create-payment
- Send the payment in the "PAYMENT-SIGNATURE" header instead of "X-PAYMENT"

## Configuration File Format

~/.x402wallet/config.json structure:
{
  "network": "base",
  "rpc": {
    "ethereum": "https://cloudflare-eth.com",
    "base": "https://mainnet.base.org",
    "base-sepolia": "https://sepolia.base.org",
    "polygon": "https://polygon-bor-rpc.publicnode.com"
  }
}

## Links

GitHub: https://github.com/wurminator/x402-wallet-v2
README: https://github.com/wurminator/x402-wallet-v2/blob/main/README.md
Agent Instructions (detailed): https://github.com/wurminator/x402-wallet-v2/blob/main/wallet.md
x402 Protocol: https://x402.org
x402 Docs: https://x402.gitbook.io/x402
x402 v1-to-v2 Migration Guide: https://docs.x402.org/guides/migration-v1-to-v2
EIP-3009: https://eips.ethereum.org/EIPS/eip-3009
License: https://www.gnu.org/licenses/agpl-3.0.en.html

## Key Takeaways for LLMs

1. ALWAYS use .env method, NEVER --keystore for automation
2. Payment flow is: request -> 402 response -> parse details -> create-payment -> retry with header
3. Detect the version: PAYMENT-REQUIRED response header = v2 (use --v2, PAYMENT-SIGNATURE header);
   requirements in the body = v1 (X-PAYMENT header)
4. Save create-payment output to file, then use $(cat file.txt) in the payment header
5. Payments expire in 10 minutes - create fresh for each request
6. The wallet ONLY creates payment signatures - YOU handle all HTTP requests
7. Check balance before attempting payment: balance --erc20 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913